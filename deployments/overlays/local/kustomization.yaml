apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dev
resources:
  - comm
  - mam
  - adm
  - ctm
  - ifm
  - web
  - gateway

patches:
  - path: patch.yaml
    target:
      kind: Deployment
